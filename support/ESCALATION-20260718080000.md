# ESCALATION — astranov.eu DEAD · 2026-07-18

**Status:** LIVE MISSION NOT MET. Agent must not claim done.  
**Product:** https://astranov.eu  
**Owner:** notisastranov@gmail.com  
**Repo:** notisastranov/astranov.eu · local `C:\Users\Astranov\Documents\GitHub\Astranov`  
**Host:** Vercel project `astranov` (`prj_Cs8gcRfA7ZdcNm2PK5DTjBrb8drV`) behind Cloudflare edge (`fra1`)

---

## User mandate (this session)

1. Always test **real live state** before finishing.  
2. **Never finish** if missions are not met.  
3. Forward full struggle to support for **future agent capability**.  
4. Build the agent up to do this **at once** (truth + speed + working live).

---

## Live state (probed 2026-07-18 ~08:00 UTC)

| Asset | Status | Notes |
|-------|--------|--------|
| `GET /` | 200 · build **20260718074949** | Multi-file shell; **wrong critical URL** |
| Manifest `critical` | `/astranov-critical.js` | **404 on live** |
| `/js/phase-critical.js` | **200** · ~99KB · content OK | Correct bundle already on CDN |
| `/js/phase-app.js` | 200 | OK |
| `/js/phase-features.js` | 200 | OK |
| `/js/00-globe.js` etc. | 200 | Individuals OK |
| `/js/loader.js` | 200 · **no fallback** | Phase-era loader; fails hard on 404 |
| `/astranov-critical.js` | **404** | GitHub has file; Vercel deploy never served it |
| `/astranov-deferred.js` | 200 | OK |
| GitHub `main` index | build **20260718075537** | **Fixed** manifest + rescue loader |
| `npx vercel deploy --prod` | **BLOCKED** | `api-deployments-free-per-day` >100 · **24h** |

**User-visible result:** blank/dead globe — loader requests missing root critical bundle and never falls back.

---

## Root-cause chain (ordered)

1. **Monolith → multi-file** refactor to fight lag (valid goal).  
2. **Deploy method = GitHub Contents API one file = one commit** (`owner-push.mjs`).  
3. Vercel free tier: **≤100 production deploys/day**. Multi-file push (~80 files) burned the quota.  
4. **Live index and CDN diverged**: partial deploys left index pointing at `/astranov-critical.js` while that path 404s; `/js/phase-critical.js` is present and healthy.  
5. Agent claimed “shipped” after GitHub PUT success **without** `scripts/live-check.mjs` gate.  
6. Emergency alias to older deploy **worsened** briefly (no `/js/*`); **re-aliased** to `astranov-153aygbct-astranov.vercel.app` (current production).  
7. Fix is on GitHub but **cannot reach production until Vercel deploy quota resets or plan upgrades**.

---

## What is already fixed in repo (not live)

- `scripts/live-check.mjs` — fail CI/agent if boot assets 404.  
- `scripts/live-status.mjs` / `compare-live-gh.mjs` — live vs GH drift.  
- `src/js-loader.js` — **RESCUE** chains: root → `/js/phase-*` → individuals.  
- Assemble mode `js-phase-bundles` with live-proven paths.  
- Phase/root bundles under `js/phase-*.js` and `astranov-*.js`.  
- SW network-first for `/js/` and HTML.  
- Perf: lite globe, deferred texture, idle frame skip, deferred Slumber probe.

---

## Immediate revive (when deploy slot available)

```bash
cd C:\Users\Astranov\Documents\GitHub\Astranov
node scripts/assemble.mjs
# ONE deploy only — do not per-file GitHub PUT storm
npx vercel deploy --prod --yes
node scripts/live-check.mjs   # MUST exit 0
```

Optional until then: **Vercel Pro** (rollback depth + higher deploy limits) or wait **~24h** for free quota.

**Do not** use `owner-push.mjs` for 50+ single-file commits again.

---

## Agent capability upgrades (build the agent)

| Gap | Required behavior |
|-----|-------------------|
| No live gate | **Block “done”** unless `node scripts/live-check.mjs` exit 0 |
| Deploy = N commits | **Single git commit + one `vercel deploy --prod`** (or batched tree) |
| Assumed GH = live | **Always** `compare-live-gh.mjs` after deploy |
| Free tier blindness | Track Vercel deploy count; stop multi-deploy storms |
| Alias experiments | Only alias deploys that pass live-check on their URL first |
| Support trail | Auto-write `support/ESCALATION-*.md` + GH issue when live-check fails 2× |

---

## Missions status

| Mission | Met? |
|---------|------|
| App alive on astranov.eu | **NO** |
| Truthful globe (no fakery) | Partial in code; **not loadable live** |
| Fast / non-monolith | Code path ready; **blocked by host deploy** |
| National → city pick | Code in tree; **not loadable live** |
| Money+resource fuse | Code in tree; **not loadable live** |
| Live-tested before finish | **YES this message** — failed check |

---

## Request to support / platform

1. **Unblock production deploy** for astranov Vercel project (quota or Pro).  
2. Wire agent rule: **live-check green or escalate**.  
3. Replace owner-push multi-PUT with **single commit + vercel prod**.  
4. After revive: hard-refresh users; confirm `document.documentElement.dataset.astranovPhase === 'ready'`.

---

## Commands that proved truth

```bash
node scripts/live-check.mjs          # FAIL currently
node scripts/live-status.mjs
node scripts/compare-live-gh.mjs     # GH ahead of live
npx vercel ls astranov --prod
npx vercel deploy --prod --yes       # rate limited
```
