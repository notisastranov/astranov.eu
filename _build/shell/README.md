# Shell build parts

Base64 chunks `p0.b64`..`pN.b64` are assembled by `.github/workflows/assemble-shell.yml` into:
- `index.html`
- `js/shell-full.html`

Source local bytes: 107241. Required markers: `super-cli-bar`, `__ASTRANOV_MANIFEST__`.
